{{- if .Values.puppetdb.enabled }}
{{- if and (not .Values.global.postgresql.auth.existingSecret) (not .Values.customPostgresql.enabled) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "puppetdb.secret" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "puppetserver.puppetdb.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.global.postgresql.auth.secretKeys.usernameKey | quote }}: {{ .Values.global.postgresql.auth.username | b64enc | quote }}
  {{ .Values.global.postgresql.auth.secretKeys.userPasswordKey | quote }}: {{ .Values.global.postgresql.auth.password | b64enc | quote }}
{{- end }}
---
{{- if .Values.puppetboard.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "puppetdb.fullname" . }}-puppetboard
type: Opaque
data:
  {{- $secret := lookup "v1" "Secret" .Release.Namespace "puppetdb-puppetboard" }}
  {{- if $secret }}
  SECRET_KEY: {{ $secret.data.SECRET_KEY }}
  {{- else }}
  SECRET_KEY: {{ randAlphaNum 64 | b64enc | quote }}
  {{- end }}
{{- end }}
{{- end }}
